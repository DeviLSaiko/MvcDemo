@model IEnumerable<DemoMvc.Models.Users>
@using DemoMvc.Models;

<div>
    <h4>Users</h4>
    <hr />
     
        @foreach (Users users in @Model)
        {
            
                     
                    <li>
                        @Html.ActionLink(users.Name, "Users", new { Id = users.UID }  )
                    </li>
           
           
           
        }
     
</div>

<p class="pt-5">
    @Html.ActionLink("Go to Departments", "Dep" )
   
</p>
